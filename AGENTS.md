# AGENTS.md

## 项目规则

### UI / 页面规则

- 所有页面必须按**响应式设计**实现，不能只针对桌面端开发。
- 响应式设计必须从页面结构层面规划，不能只在最后阶段零碎补样式。
- 每次新增或修改页面时，至少同时考虑：
  - 移动端窄屏
  - 平板/中等宽度
  - 桌面端宽屏
- 不能依赖横向滚动来完成主要阅读、主要操作或主要流程。
- 同一屏只保留一个主要目标，优先降低认知负担，避免堆叠同级功能块。
- 公共 UI 模块应尽量复用，保持视觉和交互一致性。

### 当前产品方向

- 产品定位是轻娱乐、轻解读、轻占卜体验，不是工具台、控制台或后台页面。
- 页面表达要优先面向普通用户，避免开发视角、系统视角和过度功能说明。
